import { KeyupDebouncedDirective } from './keyup-debounced.directive';

describe('KeyupDebouncedDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyupDebouncedDirective();
    expect(directive).toBeTruthy();
  });
});
